import { FiltarationPipe } from './filtaration-pipe';

describe('FiltarationPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltarationPipe();
    expect(pipe).toBeTruthy();
  });
});
